<Window x:Class="DicomDe_identificationTools.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DICOM 脱敏工具" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="输入路径: " VerticalAlignment="Center"/>
            <TextBox x:Name="InputPathBox" Width="500" Margin="5,0"/>
            <Button Content="浏览" Click="BrowseInput_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="输出路径: " VerticalAlignment="Center"/>
            <TextBox x:Name="OutputPathBox" Width="500" Margin="5,0"/>
            <Button Content="浏览" Click="BrowseOutput_Click"/>
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="LogBox" Margin="0,0,0,10" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <ProgressBar x:Name="Progress" Width="300" Height="20" Margin="0,0,10,0"/>
            <Button Content="开始脱敏" Click="StartAnonymize_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
